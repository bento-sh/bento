/**
 * This file is a "commented JSON" file. Comments are allowed, and
 * Rush's own `rush init` template opens with exactly this banner —
 * a JSONC stripper that only understands `//` chokes on it.
 */
{
  // Pinned Rush version for the workspace.
  "rushVersion": "5.100.0",
  // Workspaces use pnpm as the package manager.
  "pnpmVersion": "8.10.0",
  "projects": [
    {
      "packageName": "@migrate-rush/web",
      "projectFolder": "apps/web",
      "reviewCategory": "production",
      "shouldPublish": false
    },
    {
      "packageName": "@migrate-rush/api",
      "projectFolder": "apps/api",
      /* per-project block comment */
      "reviewCategory": "production",
      "shouldPublish": false
    }
  ]
}
