# Minimal polyglot monorepo fixture for bento e2e.
# Empty [defaults] section is valid — everything falls through to
# built-in defaults. Pre-populated so tests don't need to run
# `bento init` before exercising plan / ci / cache behaviour.
